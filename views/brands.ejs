<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Brands</title>
</head>

<body>
    <div class="brands">
        <% brands.map(brand=>{%>
            <h2>
                <%= brand.brandName %>
            </h2>
            <img src="<%= brand.logo %>" alt="Logo">
            <a href="/<%= brand.name %>"></a>
            <% }) %>
    </div>
</body>

</html>